# Server (FE uses VITE_BACKEND_URL=http://localhost:5000/api)
PORT=5000
NODE_ENV=development

# Public base URL for absolute links (images, audio) - e.g. http://localhost:5000
BASE_URL=

# Optional: extra CORS origin (defaults include localhost:5173, 5174, 5175)
# CORS_ORIGIN=

# Database
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Google Cloud API (optional - for translation & TTS)
GOOGLE_API_KEY=